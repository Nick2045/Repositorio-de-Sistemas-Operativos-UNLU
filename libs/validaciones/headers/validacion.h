#ifndef VALIDACION_H
#define VALIDACION_H

// inprime "mensaje" y valida la entrada de un entero en el rango "min" - "max" inclusive ambos
int leer_entero(const char* mensaje, int min, int max);
// inprime "mensaje" y valida la entrada de un float en el rango "min" - "max" inclusive ambos
float leer_float(const char* mensaje, float min, float max);

#endif // VALIDACION_H
